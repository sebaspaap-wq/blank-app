# Keeps the npm dependencies fresh. All updates are batched into one daily PR
# to keep the noise down.
version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
    groups:
      npm-dependencies:
        patterns:
          - "*"
