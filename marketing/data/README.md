# Performance data

Drop Meta Ads Manager and email exports here as CSV. The agent reads this folder to make
optimisation calls — kill, scale, refresh.

Empty folder = no data = the agent must say "no data" rather than estimate.

Useful Meta columns: ad name, spend, impressions, 3-second video plays, thruplays, link clicks,
CTR, purchases, purchase value, frequency, CPA.
